version: '3.5'

services:
  tanya-movie:
    image: tanya/movie:latest
    container_name: tanya-movie
    restart: always
    build:
      context: .
      dockerfile: ./nginx/Dockerfile
    command: nginx -g 'daemon off;'
    networks:
      - tanya-movie-net

networks:
  tanya-movie-net:
    name: tanya-movie-network
